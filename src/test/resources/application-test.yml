spring:
  r2dbc:
    url: r2dbc:h2:mem:///testdb;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: false
  data:
    mongodb:
      uri: mongodb://localhost:27017/testdb
    redis:
      host: localhost
      port: 6379

logging:
  level:
    root: WARN
    com.example: DEBUG
